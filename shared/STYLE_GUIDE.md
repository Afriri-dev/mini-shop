# 🎨 Conventions de code

## Python (Auth & Orders)
- Respecter PEP8.
- Utiliser `black` pour le formatage.
- Tests avec `pytest`.

## Node.js (Products)
- Utiliser ESLint avec règles standard.
- Indentation 2 espaces.
- Tests avec `jest`.

## JSON / YAML
- Indentation 2 espaces.
- Pas de trailing commas.